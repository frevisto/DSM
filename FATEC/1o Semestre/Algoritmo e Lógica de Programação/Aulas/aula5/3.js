let numeros=[1,2,3,4];
let i=0;
let pos = numeros.length - 1;
    while(
        i <= numeros.length - 1)
        { console.log(numeros[pos]),
                    pos --; i ++;}
console.log("Fim do programa");